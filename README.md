# IR project
```
Need to download it by yourself (file too large)
IRProj/post.zip
IRProj/data/post1.txt

FormattedFile.exe should be run too if formattedfile.txt doesn't exist
if not exist please compile it by command in compile
Then run BuildInvFile.exe, Retrieval.exe
follow instruction
TREC result will be output to TREC.txt
then evaluation by (change the path for your dir)
trec_sample/trec_eval_cmd.exe -o teach/comp433/2005b/assign/Query/judgerobust TREC.txt

Second approach currently working on 6 iteration, 3 words each, you can change it in (need to make variable as parameter in future)
secondApproach()
secondApproachAlgo()
```
